# دليل إعداد نظام إدارة الشاحنات

## المتطلبات الأساسية

### 1. تثبيت Node.js
قم بتحميل وتثبيت Node.js من الموقع الرسمي:
- اذهب إلى: https://nodejs.org/
- قم بتحميل النسخة LTS (الأكثر استقراراً)
- اتبع خطوات التثبيت

للتحقق من التثبيت:
```bash
node --version
npm --version
```

### 2. تثبيت Python
تأكد من تثبيت Python 3.8 أو أحدث:
```bash
python --version
```

### 3. تثبيت pip (مدير حزم Python)
```bash
pip --version
```

## خطوات الإعداد

### الخطوة 1: إعداد الخادم الخلفي (Backend)

1. انتقل إلى مجلد الخادم الخلفي:
```bash
cd flask_backend
```

2. قم بتثبيت المتطلبات:
```bash
pip install -r requirements.txt
```

3. شغل الخادم:
```bash
python run.py
```

سيتم تشغيل الخادم على المنفذ 3001
- رابط الخادم: http://localhost:3001
- رابط API: http://localhost:3001/api

### الخطوة 2: إعداد الواجهة الأمامية (Frontend)

1. افتح نافذة terminal جديدة
2. انتقل إلى المجلد الرئيسي:
```bash
cd ..
```

3. قم بتثبيت المتطلبات:
```bash
npm install
```

4. شغل الواجهة الأمامية:
```bash
npm run dev
```

سيتم تشغيل الواجهة على المنفذ 5173
- رابط الواجهة: http://localhost:5173

## بيانات الدخول الافتراضية

- **اسم المستخدم**: admin
- **كلمة المرور**: admin123

## الميزات المتاحة

### ✅ لوحة التحكم
- إحصائيات شاملة للأسطول
- رسوم بيانية تفاعلية
- نظرة عامة على الأداء

### ✅ إدارة الشاحنات
- إضافة وتعديل وحذف الشاحنات
- تتبع حالة كل شاحنة
- تفاصيل شاملة لكل شاحنة

### ✅ إدارة الرحلات
- إضافة رحلات جديدة
- تتبع تقدم الرحلات
- إدارة حالات الرحلات (نشطة، مكتملة، متأخرة)

### ✅ إدارة الصيانة
- تسجيل عمليات الصيانة
- جدولة الصيانة القادمة
- تتبع تكاليف الصيانة

### ✅ تقارير المصاريف
- تسجيل جميع المصاريف
- تصنيف المصاريف (وقود، صيانة، رسوم)
- تقارير مالية مفصلة

### ✅ الإعدادات
- إدارة المستخدمين
- إعدادات النظام
- الأمان والمصادقة

## تصدير البيانات

يمكن تصدير جميع البيانات إلى ملفات Excel:
- قائمة الشاحنات
- سجلات الصيانة
- تقارير المصاريف
- بيانات الرحلات

## استكشاف الأخطاء

### مشكلة: لا يمكن تشغيل الخادم الخلفي
**الحل:**
1. تأكد من تثبيت Python
2. تأكد من تثبيت المتطلبات: `pip install -r requirements.txt`
3. تأكد من عدم استخدام المنفذ 3001 من قبل تطبيق آخر

### مشكلة: لا يمكن تشغيل الواجهة الأمامية
**الحل:**
1. تأكد من تثبيت Node.js
2. تأكد من تثبيت المتطلبات: `npm install`
3. تأكد من عدم استخدام المنفذ 5173 من قبل تطبيق آخر

### مشكلة: لا يمكن الاتصال بالخادم
**الحل:**
1. تأكد من تشغيل الخادم الخلفي أولاً
2. تأكد من أن الخادم يعمل على المنفذ 3001
3. تحقق من إعدادات CORS في الخادم

## النسخ الاحتياطي

### قاعدة البيانات
تقع قاعدة البيانات في: `flask_backend/database/trucks.db`
قم بنسخ هذا الملف احتياطياً بانتظام.

### البيانات المحلية
يتم تخزين البيانات محلياً في المتصفح (localStorage)
لنقل البيانات بين الأجهزة، استخدم ميزة التصدير إلى Excel.

## الأمان

- تم تشفير كلمات المرور باستخدام bcrypt
- استخدام JWT للمصادقة
- حماية من CSRF
- تحقق من الصلاحيات لكل مستخدم

## الدعم

في حالة مواجهة أي مشاكل:
1. تحقق من سجلات الأخطاء في Terminal
2. تأكد من تثبيت جميع المتطلبات
3. تأكد من تشغيل الخادم والواجهة على المنافذ الصحيحة

## التحديثات

لتحديث النظام:
1. احفظ نسخة احتياطية من قاعدة البيانات
2. قم بتحديث الكود
3. أعد تشغيل الخادم والواجهة

---

**تم تطوير هذا النظام باستخدام:**
- Frontend: React + TypeScript + Tailwind CSS
- Backend: Flask + SQLite
- Authentication: JWT
- Charts: Recharts
- Icons: Lucide React 